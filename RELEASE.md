# Komari Emerald Cazi v1.0.0

这是 `cazi-cc` 基于
[Tokinx/komari-theme-emerald](https://github.com/Tokinx/komari-theme-emerald)
维护的滚动定制版本。后续更新默认继续覆盖此 `v1.0.0` Release。

## 首页节点监控

- 每个节点可独立选择并排序最多 8 个延迟监测任务。
- 首页直接显示任务名称，延迟在左、丢包在右，不再合并 IPv4、IPv6 或其他任务。
- 首页卡片统一预留 1–8 行，空位显示 `--`，所有节点卡片保持等高。
- 首页延迟/丢包统计范围可在 1–168 小时内自定义，并提供 1、6、12、24、72、168 小时快捷选项；固定 10 格会均匀覆盖完整范围，刷新间隔按范围从 1 分钟逐步放宽到 15 分钟。
- 已删除或已取消分配的首页任务会自动清理，原位置恢复为“未配置”，不再显示不存在的任务编号。
- 节点名称下方独立显示公开备注。
- 最优档默认使用醒目的 `#5EEAA6`，下一档使用 `#47B592`，全部颜色可配置。
- Emerald 设置页直接继承 Komari 后台的原生明暗模式、`data-color-theme` 和全部 CSS 配色变量，不再维护一套有色差的独立暗色方案。

## 节点详情分析

- 任务分析面板默认可见，移动端使用单列任务卡和全宽指标区域，不再使用越界悬浮框。
- 支持 1 小时、6 小时、12 小时、1 天、3 天和 7 天。
- 支持延迟/丢包合图或分图、P50/P95/P99、缩放、阈值、刷新控制及 PNG/CSV 导出。
- 浏览器使用短时缓存、请求合并，并在后台标签页暂停自动刷新。

## 线路对比

- 首页“线路对比”入口增加主题绿呼吸光；减少动态效果时保留静态高亮。
- 以延迟监测任务为第一视角，展示节点排名、P0.5-P99.5 主要波动区间、延迟/丢包散点、多节点趋势和指标明细。
- 访客侧只显示任务名称；被监测目标的 IP 地址和域名不会写入公开分析缓存或页面。
- “同任务网络质量对比指数”综合丢包、P50、P95、波动和数据覆盖率；权重、评级与最低样本门槛可在主题设置中调整。
- 评分模型 v2 提高 P50/P95 延迟权重、降低波动权重，并使用固定波动尺度；旧版评分配置会自动迁移到新的推荐值。
- 默认评级门槛收紧为优秀 95、良好 85、一般 70，约 70ms 的零丢包节点不再因为组内相对优势直接评为优秀。
- P50-P95 改为带端点、连线和数值标签的区间图，极短区间也保持可见。
- 延迟/丢包散点支持悬停或轻触查看节点名称、公开备注、P50/P95、丢包次数和评级。
- 桌面端统计卡片和左右分析栏使用统一中线与间距，消除错位和上下贴合。
- 少于 3 个有效节点时只展示指标，不给出误导性排名。
- 丢包总览使用完整聚合计数并显示“丢包次数 / 总样本数”；图表降采样不会丢失该结论。
- 新增按需“探测诊断”，展示网络可达率、HTTP 状态合格率、最小–最大延迟、大小包样本、DNS/TCP/TLS/TTFB 与连接级 TCP 重传。
- 探测诊断只读取后端已有的汇总指标并缓存 5 分钟，不会在访客打开页面时主动执行网络探测。
- 服务器定期生成共享静态缓存，访客不会重复执行全量统计；多节点趋势仅在用户打开时按需加载。

## TCP 连接质量

- 新增任务优先的 TCP 连接质量分析页，按省份、运营商和 IP 版本比较不同节点。
- 标准检测展示 SYN 首次响应丢失率、P50、P95、趋势和目标明细；实验性大小包默认关闭。
- TCP 综合分可纳入 ICMP、标准 SYN 和实验性大小包，并具备丢失率封顶、最低样本、覆盖率和同目标故障排除保护。
- 后端自动更新 TcpQuality 国内测试目录并定期生成固定快照；公开接口只返回目标标签，不暴露目录中的 IP、域名或端口。
- 访客打开 TCP 分析页不会执行 nping、主动探测或全量统计。

## ChatGPT 解锁线路

- 首页现在可并列显示“线路对比”“TCP 质量”“ChatGPT 解锁线路”，手机端按三行展示；三个入口均使用主题绿呼吸光。
- Emerald 设置可控制三个首页入口的显示、隐藏和排序，并设置 ChatGPT 分析页默认时间范围。
- Agent 使用节点实际系统 DNS 发起真实 HTTPS 请求，覆盖 DNS、Smart DNS 规则、内部入口和后方代理出口的完整链路。
- 默认每 60 秒执行一次主 Web 轻量检测，每 15 分钟验证 Web、认证、API、静态资源和 Cloudflare 路由。
- 对照 DNS 与固定入口诊断保留为可选能力并默认关闭；固定入口只用于诊断，不参与排名。
- 评分由解锁状态 40%、HTTPS 成功率 25%、TTFB 20%、TCP 与 TLS 10%、稳定性 5% 组成，并对部分可用、地区受限和高失败率设置封顶。
- 分析页提供节点排名、TTFB/HTTPS 失败分布、最小/P50/P95/最大趋势、出口国家、Cloudflare Colo 和指标明细。
- 后端按 1/6/12/24/72/168 小时预计算快照，访客访问不会触发检测；公开数据不包含域名、解析地址、DNS、内部入口或地址指纹。
- Telegram 仅在连续两轮完整验证异常后告警，连续两轮恢复后发送恢复通知。

## 隐私与最近访客

- 匿名节点接口由配套的 [cazi-cc/komari](https://github.com/cazi-cc/komari) 后端强制移除节点 IPv4、IPv6、私有备注、版本和令牌。
- 新增管理员专用“最近访客”，按需分页查看访问时间、来源 IP、站内路径和设备浏览器摘要。
- 最近访客自动显示国家/地区、城市、网络组织与 ASN；管理员可维护 IP 封禁和免通知白名单。
- 可配置 Telegram 新访客通知；已登录管理员的浏览器、白名单 IP 和静态资源请求不会触发提醒。
- 访客记录由服务器识别 IP 与 User-Agent，前端只发送不含查询参数的站内路径；接口带速率限制，日志沿用 Komari 的 30 天自动清理。
- 首页访客浮条不再调用第三方 IP/地理位置服务，只在本机识别设备类型、浏览器和访问时间。
- 后端不把监测目标、任务 DNS、解析 IP 或其短指纹写入公开指标；访客只能读取任务名称和非敏感质量统计。

## 安装

远程导入仓库：

```text
https://github.com/cazi-cc/komari-theme-emerald
```

也可下载本 Release 中固定名称的 `komari-theme-emerald.zip` 后上传到 Komari。
